/** @jsxImportSource @emotion/react */
import { Box } from '@mui/material';
import { styles } from './App.styles';

export const App = () => {
  return (
    <Box css={styles.app}>
      page
    </Box>
  )
}
